; ① 関数（function / アロー関数）
; 役割

; 👉 処理をひとまとめにする箱

; いつ使う？

; 同じ処理を何度も使うとき

; 判定ロジックを分けたいとき

; DOM操作とロジックを分離したいとき

; 頭の中の合図

; 「これ、毎回書いてない？」
; 「入力 → 結果 にしたい」

; 型（脳内テンプレ）

; const fn = (入力) => {
;   処理
;   return 結果
; };


; ② 引数（parameter）
; 役割

; 👉 関数に渡す材料

; いつ使う？

; 値が毎回変わるとき

; 外から条件をコントロールしたいとき

; 合図

; 「この値、外から渡したい」

; judgeScore(80);
; judgeScore(60);



; ③ if / else
; 役割

; 👉 条件によって処理を分ける

; いつ使う？

; 判定が必要なとき

; YES / NO

; ランク分け

; 合図

; 「場合分けが必要」

; if (score >= 70) {
;   return "合格";
; } else {
;   return "不合格";
; }

; ④ return（超重要）
; 役割

; 👉 関数の外に結果を返す

; ルール

; return した瞬間に関数は終了

; return がない → undefined

; 合図

; 「この結果、外で使いたい」

; const result = checkScore(80);

; ⑤ オブジェクトで返す
; 役割

; 👉 複数の結果をひとまとめ

; いつ使う？

; 結果 + メッセージ

; 判定 + 表示用テキスト

; APIレスポンス風

; 合図

; 「返したいものが2つ以上」

; return {
;   rank: "A",
;   message: "とても良い"
; };

; ⑥ true / false を返す関数
; 役割

; 👉 判定専用

; いつ使う？

; 条件分岐の材料にする

; if の中で使う

; const isAdult = age => age >= 18;

; if (isAdult(20)) {
;   console.log("入場OK");
; }

; 🔁 全部つながる例（超重要）
; const judgeScore = score => {
;   if (score >= 90) {
;     return { rank: "S", message: "完璧" };
;   } else if (score >= 80) {
;     return { rank: "A", message: "とても良い" };
;   } else {
;     return { rank: "C", message: "再挑戦" };
;   }
; };

; const result = judgeScore(82);
; console.log(result.rank);
; console.log(result.message);


; 👉 使ってるもの全部言える？

; 関数 → 処理をまとめる

; 引数 → 点数を渡す

; if → 条件分岐

; return → 結果を返す

; オブジェクト → 複数結果

; 🧪 セルフチェック（超重要）

; 見ずに答えて👇

; Q1

; 「return がない関数」は何を返す？
; undefined
; Q2

; 「true / false を返す関数」は何に向いてる？
; if
; Q3

; 「オブジェクトで返す」のはどんな時？
; 複数のデータを返したい時
; Q4

; 「関数の中で if を書く理由」は？
; 条件分岐するため

; const judgeage = age =>{
;     if(age>=20){
;         return "true"
;     }else{
;         "false"
;     }
; }; 

; const judgeScore = score =>{
;     if(score >= 90){
;         return{rank:"S",message:"完璧"};
;     }else if(score >=70){
;         return{ rank: "A", message: "合格" };
;     }else{
;         return{ rank: "C", message: "再挑戦" };
;     }
; };

; console.log(score(90));