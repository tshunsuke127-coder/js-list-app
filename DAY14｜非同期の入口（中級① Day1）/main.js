// DAY14｜非同期の入口（中級① Day1）
// テーマ：「なぜ非同期が必要なのか」を説明できる
// 🎯 今日のゴール（コード＋文章）

// 同期 / 非同期の違いを文章で説明できる

// setTimeout を使って「待ってる間も動く」を理解する

// 「このコードはなぜこの順で表示されるか」を言葉で説明できる

// Step0｜まず文章で考える（超重要・10分）
// ❓ 質問（声に出して or 文章で）

// Q：JavaScriptで「非同期」が必要なのはなぜ？

// 👇 今日の正解イメージ（暗記じゃなく理解）

// JavaScriptは基本的に上から順に1つずつ処理する
// でも時間がかかる処理で止まると、画面が固まる
// だから「待つ処理」は後回しにして、他を先に動かす必要がある

// ※ 今日はこれを自分の言葉で言えるようになるのが目的

// Step1｜同期処理を体感（10分）

console.log("①開始");

console.log("②開始");

console.log("③開始");

// 📝 説明してみよう（文章）

// 上から順に実行される

// ① → ② → ③ の順で必ず表示される

// 前が終わるまで次に進まない

// 👉 これが「同期」

//Step2｜非同期を体感（setTimeout）（20分）

console.log("①開始");

setTimeout(() =>{
    console.log("②２秒後");
},2000);

console.log("③終了");

// Step3｜ここが最重要（文章で説明）（20分）
// ❓ なぜこの順になる？

// 👇 模範回答（今は見てOK）

// setTimeout は「あとで実行して」と予約する
// JavaScriptは待たずに次の処理へ進む
// だから③が先に出て、時間が来たら②が実行される

// Step4｜自分の言葉で説明してみる（必須）

//書いてみよう（短くてOK）

// このコードでは、setTimeoutの中はすぐ実行されず、後で呼ばれるように登録される。
// そのため、下に書かれているconsole.log("③終了")が先に実行される。

// 👉 完璧じゃなくていい。意味が通じればOK

// Step5｜ありがちな勘違い（重要）

// ❌ 「setTimeout は別スレッドで動く」
// ⭕ JavaScriptは基本1本。後で実行されるだけ

// ❌ 「2秒待ってから下が動く」
// ⭕ 下はすぐ動く


// tep6｜DAY14 セルフチェック（文章で）
// 見ずに答えられたら合格👇

// 1️⃣ 非同期が必要な理由は？
// 重たい処理があるとそこで処理がそこで止まってそれ以降の処理が進まなくなってしまうため、非同期にした方がいい
// 2️⃣ setTimeout の中はいつ実行される？
// 例えば①が２秒後に設定されていれば先に②が実行されその後に①が実行
// 3️⃣ なぜ③が②より先に表示される？
// ②はsetTimeoutで後で実行されるようになっているため、先に③が実行される